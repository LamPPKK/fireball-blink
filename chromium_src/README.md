# Chromium source overrides

Mirror Chromium-relative paths below this directory only when an overlay include or build target cannot provide the required seam. Every override requires an owner and tests. Direct patches remain the last option and must appear in `patches/manifest.json`.
