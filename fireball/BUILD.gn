group("fireball_overlay") {
  public_deps = [
    "//fireball/browser:domain",
    "//fireball/chromium:chromium_adapter",
    "//fireball/chromium:chromium_renderer_adapter",
    "//fireball/components/adblock:adblock_policy",
    "//fireball/components/egress",
    "//fireball/components/navigation",
    "//fireball/components/privacy",
    "//fireball/components/transfer",
  ]
}

# B1 component-link gate. It compiles the browser request adapters, typed
# renderer stylesheet endpoint, document transport, lifecycle owner and async
# controller bridge against Chromium public APIs, but deliberately does not
# claim Chrome constructs them or supplies generic DOM snapshots.
executable("overlay_smoke") {
  testonly = true
  output_name = "fireball_overlay_smoke"

  sources = [ "overlay_smoke.cc" ]
  deps = [ ":fireball_overlay" ]
}
